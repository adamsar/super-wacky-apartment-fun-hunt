var chai = require('chai'),
    expect = chai.expect,
    should = chai.should;

describe('Preliminary test', function () {
  it("should pass", function () {
    chai.expect(true).to.equal(true);
  });
});